/* 賞味期限管理 — 接続先の設定（全員共通）
 *
 * ここに接続URLとパスコードを書いておくと、担当者は画面を開くだけで使える。
 * 聞かれるのは「作業する人の名前」だけになる。
 *
 * 書き換えたら GitHub に push する（GitHub Pages に反映される）。
 * 全員の画面がここを見るので、GASを再デプロイして /exec のURLが変わったときは
 * ここを直せば全員に効く。担当者側の操作は要らない。
 *
 * 注意（承知の上で固定している）:
 *   このファイルは画面と一緒に配信されるので、ページを開ける人・リポジトリを見られる人には
 *   パスコードが見える。つまりパスコードは「部外者が偶然たどり着けない」程度の鍵であって、
 *   秘密ではない。シートに人に見せられないものは置かないこと。
 *   万一漏れたときは、シートのメニュー「② パスコードを設定する」で変えて、ここも直す。
 *
 * 両方とも空にしておくと、画面が今まで通り「接続URL・パスコードを入力する画面」を出す。
 */
window.SMK_CONFIG = {
  // GAS ウェブアプリの URL（末尾が /exec）
  url: 'https://script.google.com/macros/s/AKfycbwshtW0_NTbOdGk6x5XU1ezZYEzPoZvtPdZq7b3yNmnk60wGheZX-L3mlXmtm1ruorX/exec',
  // シートのメニュー「② パスコードを設定する」で決めたパスコード
  pass: 'rising05'
};
