# セットアップ手順

上から順にやれば動く。所要 30〜40分。プログラミングの知識は要らない。
**設計の理由は [README.md](README.md) にある。**

用意するもの:

- Google アカウント（スプレッドシートを置く）
- GitHub アカウント（画面の公開と、毎朝の通知に使う）
- Slack で Incoming Webhook を作れる権限

---

## 1. スプレッドシートを用意する

このアプリは**2枚のシート**を読み書きする。列の位置は固定なので、この形にしておく。

| シート | 役割 | 列 |
|---|---|---|
| 1枚目（登録シート） | 読んだJANと選んだ賞味期限が溜まっていく | A:JAN　B:商品名　C:属性　D:賞味期限 |
| 2枚目（DBシート） | 商品の台帳。JANから商品名と属性を引く | A:JAN　B:商品名　C:属性 |

1. どちらのシートも **1行目を見出し**にする（A1 は「JAN」にすること。ここを見て
   「本当にこのシートでよいか」を確かめている）
2. すでに商品リストがあるなら、DBシートに `JAN / 商品名 / 属性` の形で貼っておく
3. **列を挿入しないこと。** 位置で読み書きしているのでずれる

> 既存のスプレッドシートを使う場合、アプリは**1枚目を登録シート・
> gid 391546061 を DBシート**として探す。違うときは手順3のあとで
> **賞味期限管理 → 使うシートを指定する** から `register=シート名` / `db=シート名` を入れる。

## 2. Apps Script にコードを貼る

1. スプレッドシートのメニューで **拡張機能 → Apps Script**
2. 左の「エディタ」で、`gas/` の中身をそれぞれ貼る

   | 貼るファイル | Apps Script 側 |
   |---|---|
   | `gas/Code.gs` | 既定の `コード.gs` を全部消して貼り付け |
   | `gas/Jan.gs` | ＋ → スクリプト → 名前を `Jan` にして貼り付け |
   | `gas/Setup.gs` | ＋ → スクリプト → 名前を `Setup` にして貼り付け |

3. **タイムゾーンを合わせる**
   左の歯車（プロジェクトの設定）→「`appsscript.json` マニフェスト ファイルをエディタで表示する」に
   チェック → エディタに出てきた `appsscript.json` を `gas/appsscript.json` の内容で置き換える。

   > `timeZone` が `Asia/Tokyo` でないと、日付が1日ずれる。

4. 保存する（Ctrl+S）

## 3. シートとパスコードを用意する

1. スプレッドシートのタブに戻って**ページを再読み込み**する
2. 上部に **「賞味期限管理」** メニューが出る（出ないときは数秒待って再読み込み）
3. **賞味期限管理 → ① 使うシートを確かめる / 整える**
   - 初回は承認を求められる。「詳細」→「（プロジェクト名）に移動」→「許可」
   - どのシートを登録シート／DBシートとして掴んだかが出る。`○` が2つ出ればよい
   - JAN列を文字列書式に、賞味期限列を日付書式に整える（13桁のJANが `4.9012E+12` に化けるのを防ぐ）
   - `ログ` シートが無ければ作られる
   - `✕` が出たら、**賞味期限管理 → 使うシートを指定する** でシート名を直す
4. **賞味期限管理 → ② パスコードを設定する**
   - 6文字以上。担当者に配る合言葉になる
   - **この値はあとで GitHub にも登録する**のでメモしておく

> 通知は既定で **7日前・3日前・前日** の3回。変えるなら
> **賞味期限管理 → 通知する日を変える** で `7,3,1` のように入れる。
> （「以内」ではなく「ちょうどその日数」なので、同じ商品が毎日通知されることはない）
>
> 商品名の自動取得を使うなら、ここで
> **賞味期限管理 → JANコードDBのAPIキーを設定する** も済ませておく。
> `yahoo=（Yahoo!のアプリケーションID）` または `rakuten=（楽天のアプリID）` の形式で入力する。
> 設定しなくても、キー不要の Open Food Facts と手入力で運用できる。

## 4. ウェブアプリとして公開する

Apps Script のエディタに戻って:

1. 右上の **デプロイ → 新しいデプロイ**
2. 歯車 → **ウェブアプリ**
3. 設定を次のとおりにする

   | 項目 | 値 |
   |---|---|
   | 次のユーザーとして実行 | **自分** |
   | アクセスできるユーザー | **全員** |

   > 「全員」にするのは、GitHub Actions が Google にログインできないため。
   > URL を知っているだけでは何もできず、**パスコードが無いと拒否される**（`ping` を除く）。

4. **デプロイ** を押し、表示された **ウェブアプリのURL**（`.../exec` で終わる）をメモする

**動作確認:** そのURLの末尾に `?action=ping` を付けてブラウザで開く。
`{"ok":true,...}` と JSON が返れば成功。`passcodeSet` が `true` になっていることも確認する。

> コードを直したら、**デプロイ → デプロイを管理 → 鉛筆マーク → バージョン「新バージョン」→ デプロイ**
> で更新する。「新しいデプロイ」を作り直すと URL が変わり、全員の設定をやり直すことになる。

## 5. GitHub にリポジトリを作って画面を公開する

1. GitHub で新しいリポジトリを作る（**Public**。Private だと Pages が有料プラン扱いになる）

   > **このリポジトリの `docs/config.js` にはパスコードが入る。** Public にする以上、
   > パスコードは「部外者が偶然たどり着かないための鍵」であって秘密ではない。
   > **シートに人に見せられないものを置かないこと。**
   > `.env` のような本物の認証情報（APIキー等）はここに置かない
   > （JANコードDBのキーは GAS のスクリプトプロパティに入れる）。

2. このフォルダを push する

   ```
   git init
   git add .
   git commit -m "賞味期限管理"
   git branch -M main
   git remote add origin https://github.com/<ユーザー名>/<リポジトリ名>.git
   git push -u origin main
   ```

3. リポジトリの **Settings → Pages**
   - Source: **Deploy from a branch**
   - Branch: **main** / **`/docs`** → Save
4. 1〜2分待つと `https://<ユーザー名>.github.io/<リポジトリ名>/` が開くようになる

**これが担当者に配るURL。**

## 6. 接続先を config.js に書く

**ここが担当者の入力を無くしている場所。** `docs/config.js` を開いて2つ埋める。

```js
window.SMK_CONFIG = {
  url: 'https://script.google.com/macros/s/AKfy.../exec',   // 手順4でメモしたURL
  pass: 'ここに手順3で決めたパスコード'
};
```

push すると1〜2分で全員の画面に効く。担当者側の操作は要らない。

```
git add docs/config.js
git commit -m "接続先を設定"
git push
```

| | |
|---|---|
| GASを再デプロイして URL が変わった | ここの `url` を直して push（担当者は何もしなくてよい）|
| パスコードを変えた | メニュー「② パスコードを設定する」と**ここの `pass` の両方**を直す |
| 両方を空のままにした | 画面が「接続URL・パスコードを入力する画面」を出す（別シートで試すとき用）|

確認する:

1. スマホかPCのブラウザで Pages の URL を開く
2. **名前だけを聞かれる**画面が出る（URL・パスコードを聞かれたら `config.js` が空か、push がまだ）
3. 画面の下に「接続先: （スプレッドシート名）」が出ることを確かめる
4. 名前を入れて「始める」→ 本体が出れば成功
5. バーコードを1つ読んで、登録まで通しておく

> 名前は**開くたびに聞く**（共用端末で前の人の名前が残らないようにするため）。
> 一度使った名前はボタンとして残るので、次からは1タップで選べる。
> スマホでは「ホーム画面に追加」しておくとアプリのように開ける。

## 7. Slack の Incoming Webhook を作る

1. https://api.slack.com/apps → **Create New App** → From scratch
2. 名前（例: 賞味期限通知）とワークスペースを選ぶ
3. 左メニュー **Incoming Webhooks** → トグルを **On**
4. **Add New Webhook to Workspace** → 通知したいチャンネルを選んで許可
5. 表示された `https://hooks.slack.com/services/...` をコピーする

## 8. GitHub に設定を登録する

リポジトリの **Settings → Secrets and variables → Actions**

**Secrets タブ**（New repository secret）:

| 名前 | 値 |
|---|---|
| `GAS_URL` | 手順4の `.../exec` のURL |
| `GAS_PASSCODE` | 手順3のパスコード |
| `SLACK_WEBHOOK_URL` | 手順7の Webhook URL |

**Variables タブ**（任意）:

| 名前 | 値 |
|---|---|
| `APP_URL` | 手順5の Pages のURL。通知に「一覧を開く」リンクが付く |
| `NOTIFY_WHEN_EMPTY` | `true` にすると対象0件でも毎日「異常なし」を送る（既定は送らない） |

## 9. 通知を確かめる

1. リポジトリの **Actions** タブ → 左の **「賞味期限チェック → Slack通知」**
2. **Run workflow** →
   - まず `dry_run` を **チェックしたまま**（既定）実行する
     → Slack には送らず、対象件数が実行ログの下に表示される
   - 内容が正しければ、もう一度 `dry_run` の**チェックを外して**実行する
     → Slack に届く

> 以降は**毎朝 9:00（JST）に自動で送られる**。
> 定期実行は常に本番送信で、DRY-RUN にはならない。
> GitHub の混雑で数分〜数十分ずれることがある。

### 通知の中身

賞味期限まで **ちょうど 7日 / 3日 / 1日（前日）** の商品だけが載る。
1つの商品につき最大3回で、毎日同じものが並ぶことはない。
**期限切れの商品だけは、日数にかかわらず毎回載る**（消えるまで放置されないように）。

```
■ 賞味期限のお知らせ
  基準日 2026-08-27 ／ 7日前・3日前・前日に通知
  🚨 期限切れ 2件
  • 明治おいしい牛乳 900ml — 3日超過（2026-08-24）× 2
  ──────────────
  ⏰ 明日まで（2026-08-28） 2件
  • 食パン 6枚切 × 2
  • サラダチキン プレーン × 5
  ──────────────
  ⏰ あと3日（2026-08-30） 1件
  • ヨーグルト 400g × 6
```

### 設定を変えたいとき

| 変えたいもの | やること |
|---|---|
| 通知する日（7日前・3日前・前日） | メニュー **賞味期限管理 → 通知する日を変える** → `7,3,1` の形式で入力 |
| 通知の時刻（9:00） | `.github/workflows/expiry-notify.yml` の `cron: '0 0 * * *'` を直して push。**UTCなので JST から9時間引く**（例: JST 17:00 → `0 8 * * *`） |
| 画面で色が変わる日数 | メニュー **賞味期限管理 → 画面で色が変わる日数を変える**（通知には影響しない） |

---

## 担当者に配るときに伝えること

```
1. このURLを開いてください:  https://<ユーザー名>.github.io/<リポジトリ名>/
2. 開くと名前を聞かれます。ご自分の名前を入れて「始める」を押してください
     ・登録した人としてシートに残ります
     ・一度使った名前はボタンで出るので、次からは押すだけです
     ・開くたびに聞きます（共用の端末で前の人の名前が残らないように）
     ・途中で交代するときは 右上の歯車 →「人を変える」
3. スマホなら「ホーム画面に追加」しておくと次から早いです
4. 使い方
     [登録] タブ … バーコードを読む → 商品名と属性を確認 → 賞味期限を選ぶ → 登録する
                    ・DBに無い商品は「商品名を入力」「属性を入力」と出るので入れてください
                    ・入れた商品はDBにも足されるので、次からは名前が出ます
     [一覧] タブ … 見出しを押すと並べ替え。赤は期限切れ、黄は1週間以内
                    ・見るだけです。直すときはスプレッドシートを開いてください
```

担当者に伝えるのは**このURLだけ**でよい（接続先とパスコードは画面に入っている）。

---

## 更新のしかた

| 直したもの | やること |
|---|---|
| 画面（`docs/`） | `git push` するだけ。1〜2分で反映される |
| 接続先・パスコード | `docs/config.js` を直して `git push`。全員の画面に効く |
| GAS（`gas/`） | Apps Script に貼り直す → **デプロイ → デプロイを管理 → 鉛筆 → 新バージョン → デプロイ**（URLは変わらない） |
| 通知（`.github/`） | `git push` するだけ |

変更したら、手元で確認してから push する:

```
npm test
powershell -ExecutionPolicy Bypass -File tests\e2e\run.ps1
```
